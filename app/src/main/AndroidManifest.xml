<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.gestionstockapp">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GestionStockApp"
        tools:targetApi="31">

        <!-- ✅ Activité de connexion (écran de démarrage) -->
        <activity
            android:name=".view.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".view.RegisterActivity" />


        <!-- ✅ Les autres activités -->
        <activity android:name=".view.MainActivity" android:exported="true" />
        <activity android:name=".view.AddProductActivity" android:exported="true" />
        <activity android:name=".view.EditProductActivity" android:exported="true" />
        <activity android:name=".view.SearchActivity" android:exported="true" />
        <activity android:name=".view.StatsActivity" android:exported="true" />
        <activity android:name=".view.UserListActivity" />

    </application>
</manifest>
